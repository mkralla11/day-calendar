import React from 'react';
import {shallow} from 'enzyme';
// import Event from './Event';

describe('example', () => {
  it('should work', () => {
    // const event = {
    //   left: 0,
    //   top: 120,
    //   width: 300,
    //   height: 50,
    //   id: 'the id'
    // }
    // const wrapper = shallow(<Event {...event} />);
    // // const actual = wrapper.find('.event');

    // expect(wrapper).to.have.style("top", 120);
  });
})